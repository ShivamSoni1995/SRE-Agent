steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'Dockerfile.streamlit'
      - '-t'
      - 'gcr.io/$PROJECT_ID/opensre-ui'
      - '.'

images:
  - 'gcr.io/$PROJECT_ID/opensre-ui'
